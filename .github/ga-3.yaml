name: trigger

on: 
  workflow_dispatch:
    inputs:
      username:
        description: 'A username passed from the caller workflow'
        default: 'john-doe'
        required: false
        type: string




jobs:
  say-hellow:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "Hello, World!"